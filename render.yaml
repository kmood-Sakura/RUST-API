#render.yaml
services:
  - type: web
    name: my-rust-api
    env: rust
    buildCommand: cargo build --release
    startCommand: ./target/release/rust-api
    envVars:
      - key: RUST_LOG
        value: info